#ifndef I3D_SHIPCONSTANTS_H
#define I3D_SHIPCONSTANTS_H

float constexpr MOUSE_ROTATION_SPEED = 40; // when ship rotates due to camera input
float constexpr BARREL_ROLL_SPEED = 150; // when ship rotates due to keyboard input (A/D)

float constexpr WARNING_RADIUS = 500;
float constexpr COLLISION_RADIUS = 5;
float constexpr SHIP_SPEED = 400;
float constexpr SHIP_SCALE = 10;
float constexpr SHIP_ACCELERATION = 200;

float constexpr SHIP_FIRE_RATE = 0.1; // seconds per bullet

#endif